# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "No npm build needed for static HTML."
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_USER_NAME_GENERATOR }}'
          channelId: live
          projectId: user-name-generator
          args: deploy --only hosting,functions # <--- THIS LINE MUST BE INDENTED TO ALIGN WITH THE 'with:' parameters above it.